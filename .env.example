# LazyMailBOSS Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Server Configuration
# ============================================

# Port for the web dashboard and API server
# Default: 3000
PORT=3000

# ============================================
# Security Configuration
# ============================================

# Encryption key for storing email credentials securely
# IMPORTANT: Change this to a strong, random key in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=change-this-to-a-secure-random-key-in-production

# ============================================
# Database Configuration
# ============================================

# Path to the main application database
# Default: ./data/lazymail.db
DATABASE_PATH=./data/lazymail.db

# Path to the configuration database
# Default: ./data/config.db
CONFIG_DATABASE_PATH=./data/config.db

# ============================================
# Email Configuration (Optional)
# ============================================
# These can be configured via the web dashboard instead
# If set here, they will be used as initial defaults

# IMAP Configuration
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USER=your-email@gmail.com
# IMAP_PASSWORD=your-app-password

# SMTP Configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ============================================
# Auto-Reply Configuration (Optional)
# ============================================

# Check interval in seconds (how often to check for new emails)
# Default: 10
# CHECK_INTERVAL=10

# Enable manual confirmation mode (true/false)
# When true, replies require approval before sending
# Default: true
# MANUAL_CONFIRMATION=true

# Default reply template
# REPLY_TEMPLATE=Thank you for your email. I will respond shortly.

# ============================================
# Filter Configuration (Optional)
# ============================================

# Enable keyword filtering (true/false)
# Default: false
# KEYWORDS_ENABLED=false

# Comma-separated list of keywords to match
# KEYWORDS=urgent,important,meeting

# Comma-separated list of domains to exclude from auto-reply
# EXCLUDED_DOMAINS=noreply.com,notifications.example.com

# ============================================
# Logging Configuration
# ============================================

# Log level (error, warn, info, debug)
# Default: info
LOG_LEVEL=info

# ============================================
# Node Environment
# ============================================

# Node environment (development, production, test)
# Default: development
NODE_ENV=development
